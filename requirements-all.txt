# =============================================================
# Vocal Subtitle — 全量依赖 (一键安装所有功能)
# =============================================================
# 安装: pip install -r requirements-all.txt
#
# 包含: CLI + GPU 加速 + Silero VAD + LLM 优化 + 本地 NLP +
#       UVR 分离 + Spleeter + Open-Unmix + WebRTC VAD +
#       faster-whisper ASR + Web GUI + 说话人分离 + 开发工具
#
# 说明:
#   - Spleeter 仅在 Python < 3.12 时安装
#   - torch/torchaudio 请根据 GPU 情况选择安装源:
#       GPU (CUDA): pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#       CPU:        pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#       或使用 install.sh 自动检测
# =============================================================

# ---- 核心数值计算 ----
numpy>=1.24

# ---- 音频处理 ----
pydub>=0.25

# ---- 字幕构建与导出 (SRT / VTT / ASS) ----
pysubs2>=1.7

# ---- CLI 框架 ----
click>=8.0

# ---- 进度显示 ----
tqdm>=4.65

# ---- 结构化日志 ----
structlog>=23.0

# ---- 配置管理 ----
pyyaml>=6.0

# ---- 磁盘缓存 ----
diskcache>=5.6

# ---- GPU 显存监控 ----
GPUtil>=1.4; python_version < '3.12'

# ---- 核心 ASR 引擎 (CTranslate2 加速) ----
faster-whisper>=1.0

# ---- Silero VAD 引擎 (PyTorch) ----
torch>=2.0
torchaudio>=2.0

# ---- GPU 推理加速 ----
ctranslate2>=4.0

# ---- LLM 字幕优化 + 云端合并裁决 ----
openai>=1.0
tenacity>=8.0
json-repair>=0.25

# ---- 本地 NLP 语义合并 (方案五 Fast-Slow Path, CPU 推理) ----
sentence-transformers>=3.0

# ---- UVR 高品质分离 (BS-RoFormer, 默认推荐) ----
audio-separator>=0.18

# ---- Spleeter 分离引擎 (已停维, 仅 Python < 3.12) ----
spleeter>=2.3; python_version < '3.12'

# ---- Open-Unmix 分离引擎 ----
openunmix
soundfile>=0.12

# ---- WebRTC VAD 引擎 (轻量备选) ----
# 注意: Python 3.12 环境若安装 webrtcvad 失败，请先降级 setuptools:
#   pip install "setuptools<82"
#   pip install webrtcvad>=2.0
webrtcvad>=2.0
setuptools<82; python_version >= '3.12'

# ---- 说话人分离 ----
librosa>=0.10
scikit-learn>=1.3
scipy>=1.11

# ---- 说话人嵌入模型 (提高声学聚类精度) ----
# speechbrain 默认模型 (Apache 2.0, 无需签署协议)
speechbrain>=1.0
# pyannote.audio 高精度模型 (需签署协议)
# pyannote.audio>=3.0

# ---- Web GUI 图形界面 ----
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
cryptography>=41.0
websockets>=12.0

# ---- 开发测试 ----
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
black>=23.0
ruff>=0.1
mypy>=1.0
